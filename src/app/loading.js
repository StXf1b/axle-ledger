import AppLoading from "@/components/ui/AppLoading";

export default function Loading() {
	return (
		<AppLoading
			title="Loading workspace"
			description="Preparing your dashboard and syncing workshop data."
		/>
	);
}
